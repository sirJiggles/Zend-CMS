<?php
// Show all the ocntent in the system first
?>


<?php if (isset($this->content)) : ?>


<div id="left-content-col">
    <h3>Current Content</h3>
    <?php foreach($this->content as $type => $contentItem): ?>

        <div class="content-type-container">
            <h4><?php echo ucfirst($type); ?></h4>

            <?php
            if (isset($contentItem[0])):
                foreach($contentItem[0] as $item):

                    $contentData = unserialize($item->content);
                    echo '<ul>';
                    foreach ($contentData as $key => $val):

                        echo '<li>';
                        echo '<strong>'.ucfirst($key) .':</strong> '.$val[0];
                        echo '</li>';
                    endforeach;
                    
                    // Get the link for editing and removing the item
                    echo '<li><a href="/cms/content/edit/id/'.$item->id.'">Edit</a></li>';
                    echo '<li><a href="/cms/content/remove-confirm/id/'.$item->id.'">Remove</a></li>';
                    echo '</ul>';
                endforeach;
            endif;
            ?>
        </div>

    <?php endforeach; ?>
    <ul>
    <?php endif; ?>
</div>

<div id="right-side-home-page">
    <h2>Willkommen!, marvel at the current content!</h2>
</div>
