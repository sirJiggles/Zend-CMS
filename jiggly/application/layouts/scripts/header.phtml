<?php $auth = Zend_Auth::getInstance(); ?>
<header data-role="header" data-add-back-btn="true">
    <h1><?php if($this->pageTitle){echo $this->pageTitle;}else{echo '<a href="/" >Jiggly CMS</a>';}?></h1>
    <?php if ($auth->hasIdentity()): ?>
    <?php $identity = $auth->getIdentity(); ;?>
    <div id="right-side">
        <p>Hello again: <?php echo $identity->first_name .' '. $identity->last_name; ?></p>
        <a data-role="button" data-theme="c" href="/logout" title="Logout">Logout</a>
    </div>
    <div id="left-side">
        <nav>
            <a data-role="button" title="Home" data-theme="b" href="/">Home</a>
            <?php if ($identity->role == 'admin'): ?>
                <a data-role="button" data-theme="b" title="Manage Users" href="/user/manage">Manage Users</a>
            <?php endif; ?>
        </nav>
    </div>
    
    <?php endif; ?>

</header>